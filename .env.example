# Required for /api/generate (server-side only)
GEMINI_API_KEY=your_google_gemini_api_key_here

# Optional: set only when API is on a different host than the frontend
# VITE_BACKEND_URL=https://your-app.vercel.app
